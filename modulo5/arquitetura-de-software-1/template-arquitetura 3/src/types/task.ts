export type task = {
    id: string
    title: string
    description: string
    deadline: string
    user_id: string
}

export type taskInput = {
    title: string
    description: string
    deadline: string
    user_id: string
 }